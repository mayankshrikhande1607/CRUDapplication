import { ComdirectiveDirective } from './comdirective.directive';

describe('ComdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ComdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
